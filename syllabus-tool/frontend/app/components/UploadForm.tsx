import React from 'react';

export default function UploadForm() {
    return (
        <form>
            <input type="file" />
            <button type="submit">Upload</button>
        </form>
    );
}
